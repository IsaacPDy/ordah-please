{
  "name": "@ordah-please/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@ordah-please/domain": "0.1.0"
  },
  "scripts": {
    "prebuild": "npm run build --workspace @ordah-please/domain",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run --config ../../vitest.config.ts --project shared-packages",
    "pretypecheck": "npm run build --workspace @ordah-please/domain",
    "typecheck": "tsc --noEmit"
  }
}
