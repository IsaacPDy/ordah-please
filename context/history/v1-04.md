# V1-04 Integrate Clerk Google sign-in across Android and web and map identities into Neon

Deprecated: V1-04 shipped Clerk-based authentication and was replaced by V1-04A. The external Clerk development application was deleted on 2026-07-30 once V1-04A was permanently squash-committed. See `v1-04a.md`.

## Session Notes

- V1-04 used the primary checkout on `task/V1-04-clerk-authentication`. The local Clerk publishable keys and server secret were present outside Git, and the Clerk webhook signing secret was saved only in the Vercel Preview environment without being displayed or committed.
- V1-04 Task 3.1 has RED-first focused tests for unauthenticated Clerk rejection, internal identity loading and role mapping, authorization denial, the required route-execution sequence, stable HTTP error mapping, and internal-detail suppression.
- V1-04 Task 3.1 verified real Standard Webhooks signatures through Clerk's backend API, rejected forged or malformed callbacks with a safe 400, returned a safe retryable 500 on synchronization failure, and mapped `user.created`, `user.updated`, and `user.deleted` into timestamp-aware Neon upsert/archive commands. Live temporary-schema tests proved the unique audit claim and user upsert commit together, a retry created neither a second audit event nor a second mutation, and an older delayed create could not resurrect an already-deleted Clerk identity. Provider-mode test bodies used a 30-second timeout while local unit tests retained the 5-second default.
- V1-04 integrated verification passed 159 provider-free Vitest tests, 7 mobile Jest tests, 43 server tests with all Neon provider suites enabled, all workspace type checks and lint checks, Drizzle migration validation, every package build, the 13-route Next.js production build including `/api/webhooks/clerk`, task-owned formatting, whitespace validation, and a built-client scan with no server variable names.
- The development Neon database had the complete 32-table V1-03 baseline and the V1-04 `audit_events.idempotency_key` migration recorded in Drizzle's two-entry ledger. An unrelated empty `public.branches` bootstrap artifact was verified to contain zero rows and removed before the repository migrations were applied and independently checked.
- Commit `3799ad1` deployed successfully to the V1-04 Vercel branch preview at `https://ordah-please-web-git-task-v1-04-clerk-authentication-isaacpdy.vercel.app`. Vercel Authentication returned 401 before unsigned webhook requests reached the route, so Clerk setup was paused until Preview received an approved deployment-protection bypass or was deliberately made public.
- The user confirmed the dedicated Vercel automation bypass was created and `CLERK_WEBHOOK_SIGNING_SECRET` was saved without sharing either value. Commit `dfae2c0` deployed READY on the V1-04 branch preview with that Preview environment. Clerk's signed `user.created`, `user.updated`, and `user.deleted` examples each returned HTTP 200. A read-only Neon check found the three expected internal identities, the deletion archived, and one idempotent immutable audit row for each event type.
